import { Stack } from 'expo-router';

const VerifyEmailLayout = () => {
  return (
    <Stack
      screenOptions={{
        headerShown: false,
      }}
    />
  );
};

export default VerifyEmailLayout;
